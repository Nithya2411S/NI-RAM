---
/**
 * index.astro
 * Entry point. Mounts AppShell with client:load.
 * AppShell handles sidebar + section routing internally.
 */
import Layout from '../layouts/Layout.astro';
import AppShell from '../components/layout/AppShell';
---

<Layout title="NIRAM — Vedic Astrology">
  <AppShell client:load />
</Layout>
